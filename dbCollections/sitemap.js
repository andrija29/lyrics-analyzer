//This is just idea, WIP

/*var mongoose = require('mongoose');
var Schema = mongoose.Schema;

var site = new Schema({
    name: String,
    link: String
});

module.exports = mongoose.model("site", site);*/